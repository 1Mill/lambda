{
  "name": "@1mill/lambda",
  "version": "0.0.10",
  "description": "Simple wrapper around the `@aws-sdk/client-lambda` to invoke a Lambda function to reduce boilerplate.",
  "main": "dist/index.js",
  "source": "src/index.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rm -rf ./dist && microbundle --format cjs",
    "dev": "microbundle watch"
  },
  "repository": "1mill/lambda",
  "keywords": [
    "lambda"
  ],
  "homepage": "https://github.com/1mill/lambda",
  "authors": [
    "Erik Ekberg <nightw0lf@hotmail.com>"
  ],
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.25.0"
  },
  "devDependencies": {
    "microbundle": "^0.13.3"
  }
}
